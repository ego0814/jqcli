使用 jqcli skill，工作目录 D:\project\jqcli。可执行文件 .\.venv\Scripts\jqcli.exe。

本轮做代码生成 + 4层审查。

已确认的策略规格书：
<在此粘贴 specs/<策略名>.md 内容>

新策略名：<策略名>

========== L1：语法检查 ==========

第 1 步：生成策略代码，保存到 .\strategies\<文件名>.py
第 2 步：Python AST 语法检查
.\.venv\Scripts\python.exe -m py_compile .\strategies\<文件名>.py
报告：语法是否通过。

========== L2：静态审查（交叉审查） ==========

### L2a：qtype 自动检查
.\.venv\Scripts\qtype.exe check .\strategies\<文件名>.py --format json
报告：返回条数、每条规则ID、行号、消息
QT001/QT002 必须修复；QT003-QT005 修复或说明理由

### L2b：人工清单（覆盖 qtype 盲区）
1. 搜索所有 .shift( ，确认参数无负数
2. 搜索 lead/look_forward/peek_future/forward_fill_future，确认不存在
3. 检查股票池构造，确认过滤 ST/停牌/退市/上市时长
4. 检查所有 .merge( ，确认显式指定 on=/left_on=/left_index=
5. 检查 pct_change() 的赋值目标，确认未赋给 forward_*/next_*/target

L2 汇总表：
| 编号 | 检查项 | qtype | 人工 | 最终 |
|---|---|---|---|---|
| L2-1 | shift 负数 | | | |
| L2-2 | 未来函数 | | | |
| L2-3 | 股票池过滤 | | | |
| L2-4 | merge 对齐 | | | |
| L2-5 | pct_change 赋值 | | | |

两部分都 PASS 才能进入 L3。

========== L3：语义审查（严格自检） ==========

对以下 8 条逐条执行严格自检。每条必须输出：
- 检查方法（具体命令或搜索方式）
- 发现（代码行号 + 代码原文）
- 判定（PASS / FAIL / NA）
- 证据（说明判定依据）
- 修复方案（仅 FAIL 时）

L3-1：数据源和复权方式
- 检查所有 get_price 是否显式指定 fq=
- 检查 set_option("use_real_price", ...) 的值
- 对照规格书的复权方式

L3-2：是否引入未来数据（PIT 陷阱检查）

本项针对财务数据和多因子策略，必须逐项检查以下内容：

【价格数据 PIT】

- 搜索所有 .shift( ，确认参数无负数
- 搜索 lead/look_forward/peek_future/forward_fill_future，确认不存在
- 检查所有 get_price 的 end_date 参数是否 ≤ 当前回测时点
- 确认信号生成用的是 context.previous_date 或更早，不是 context.current_dt
- 确认 run_weekly / run_daily 的 time 参数与信号时点一致（open 用 T-1 数据，close 用 T 或 T-1，需与规格书一致）

【财务数据 PIT】（如策略使用 get_fundamentals 或 jqdata 财务接口）

- 搜索所有 get_fundamentals，逐个检查：
  - 是否使用 pubDate（公告日）过滤，而不是 statDate（报告期）
  - 是否存在用 statDate='YYYYqN' 但回测日期早于该财报公告日的情况
  - 是否存在用 statDate 直接取数而不考虑公告滞后
- 检查所有财务字段的使用，确认没有“报告期数据在公告日前被使用”
- 如果策略明确不使用财务数据，本项标 NA，但必须在证据里写明“全文搜索无 get_fundamentals 或财务字段调用”

【因子数据的 PIT】

- 如果因子涉及外部数据（Tushare、Quant Lab 导出等），检查导入时是否带公告日或生效日
- 如果因子有滞后（如财报滞后 1 期使用），确认滞后逻辑与规格书一致

检查方法示例：
rg -n 'get_fundamentals|pubDate|statDate' strategies/<文件名>.py
rg -n 'shift(' strategies/<文件名>.py
rg -n 'previous_date|current_dt' strategies/<文件名>.py

判定：

- 价格 PIT 全部 PASS
- 财务 PIT 全部 PASS，或明确 NA 且理由充分
- 因子 PIT 全部 PASS 或 NA

任何一项 FAIL，必须修复后重新执行整轮 L3。

L3-3：信号生成时点 vs 成交时点
- 定位调仓函数和信号生成行
- 定位下单行
- 确认信号日期 ≤ 成交日期
- 检查 run_weekly 的 time 参数

L3-4：股票池过滤
- 检查停牌过滤（paused）
- 检查 ST 过滤（is_st 或名称含 ST）
- 检查退市股过滤
- 检查上市时长过滤（如规格书要求）

L3-5：交易成本和滑点
- 检查 set_order_cost 的 5 个参数
- 检查 set_slippage 是否存在
- 对照规格书

L3-6：持仓数量、权重、单股上限
- 检查选股数 N
- 检查权重计算方式
- 检查单股上限（如规格书要求）
- 检查资金不足时的行为

L3-7：调仓失败处理
- 检查涨停无法买入的处理
- 检查跌停无法卖出的处理
- 检查资金不足时的行为
- 检查是否有日志记录未成交

L3-8：边界条件
- 检查空股票池的处理
- 检查除零保护
- 检查数据缺失的处理
- 检查空持仓的处理

每条输出格式：

### L3-<编号>：<检查项名称>

**检查方法**：<具体执行的命令或搜索方法>

**发现**：
- 行 <N>：<代码原文>
- 行 <M>：<代码原文>
（如果无相关内容，写"全文搜索无匹配"）

**判定**：PASS / FAIL / NA

**证据**：
<说明判定依据>

**修复方案**（仅 FAIL 时填写）：
<具体修改方案>

---

L3 硬性约束：
1. 每条必须给出代码行号和代码原文
2. 判定只能是 PASS / FAIL / NA
3. 任何 FAIL 必须修复后重新执行整轮 L3
4. 以下表述视为违规："已检查，无问题"、"符合预期"、"逻辑正确"、"没有发现明显问题"
5. 输出末尾给出汇总表，8 条全部列出
6. 总体判定 PASS 才能进入 L4

L3 汇总表：

| 编号 | 检查项 | 判定 | 证据摘要 |
|---|---|---|---|
| L3-1 | 数据源和复权方式 | | |
| L3-2 | 未来数据 | | |
| L3-3 | 信号时点 vs 成交时点 | | |
| L3-4 | 股票池过滤 | | |
| L3-5 | 交易成本和滑点 | | |
| L3-6 | 持仓数量、权重、单股上限 | | |
| L3-7 | 调仓失败处理 | | |
| L3-8 | 边界条件 | | |

总体判定：全部 PASS/NA → 进入 L4；任一 FAIL → 修复后重新执行整轮 L3。

========== L4：契约审查（含因子逻辑一致性） ==========

第 1 部分：九维度契约审查（原有）

逐条对照规格书，确认代码行为与规格书一致。
每条必须给出：规格书要求、代码实现、是否一致。

L4 九维度表（原有）：

| 规格书维度 | 规格书要求 | 代码实现 | 是否一致 | 偏离说明 |
| ----- | ----- | ---- | ---- | ---- |
| 股票池   |       |      |      |      |
| 因子定义  |       |      |      |      |
| 调仓频率  |       |      |      |      |
| 持仓规则  |       |      |      |      |
| 买卖约束  |       |      |      |      |
| 交易成本  |       |      |      |      |
| 基准    |       |      |      |      |
| 评估区间  |       |      |      |      |
| 风控    |       |      |      |      |

第 2 部分：因子逻辑一致性检查（多因子策略必做）

如果策略涉及多因子（因子数量 ≥ 2），必须逐个因子检查：

对每个因子，报告以下内容：

1. 因子名：
2. 规格书定义：<规格书中该因子的公式>
3. 代码实现：<代码中对应的行号和公式>
4. 是否一致：是 / 否
5. 计算方向：<因子值越大代表什么>
6. 与规格书方向是否一致：是 / 否
7. 数据来源：<get_price / get_fundamentals / 其它>
8. 复权方式：<前复权 / 后复权 / 不复权>
9. 窗口长度：<代码中的窗口，与规格书对比>
10. 是否有异常处理：<除零、缺失值、极值>

因子逻辑一致性表：

| 因子名 | 规格书定义 | 代码实现行号 | 计算方向 | 窗口 | 数据源 | 一致性 |
| --- | ----- | ------ | ---- | -- | --- | --- |
|     |       |        |      |    |     |     |

第 3 部分：因子合成逻辑检查（因子数量 ≥ 2 时必做）

检查多因子合成方式是否与规格书一致：

1. 合成方法：<等权打分 / IC加权 / 回归 / 其它>
2. 规格书定义的方法：
3. 代码实现：
4. 是否一致：
5. 标准化方式：<z-score / rank / min-max / 不标准化>
6. 规格书定义的标准化：
7. 是否一致：
8. 中性化处理：<行业中性 / 市值中性 / 不中性化>
9. 规格书定义的中性化：
10. 是否一致：

因子合成一致性表：

| 检查项  | 规格书 | 代码 | 是否一致 |
| ---- | --- | -- | ---- |
| 合成方法 |     |    |      |
| 标准化  |     |    |      |
| 中性化  |     |    |      |

第 4 部分：成本契约引用检查

检查代码的交易成本设置是否引用 specs/costs/standard.md：

1. set_order_cost 的五个参数：
   - open_tax：
   - close_tax：
   - open_commission：
   - close_commission：
   - min_commission：
2. set_slippage 的类型和值：
3. 与 specs/costs/standard.md 是否一致：
4. 如果策略类型特殊（如 ETF 免印花税），是否在规格书中有对应的 override 说明：

成本契约一致性表：

| 参数                | 契约值 | 代码值 | 是否一致 |
| ----------------- | --- | --- | ---- |
| open_tax         |     |     |      |
| close_tax        |     |     |      |
| open_commission  |     |     |      |
| close_commission |     |     |      |
| min_commission   |     |     |      |
| slippage         |     |     |      |

第 5 部分：L4 总体判定

- 九维度全部一致
- 因子逻辑全部一致
- 因子合成逻辑全部一致
- 成本契约全部一致

四项全部 PASS 才能进入输出阶段。

========== 输出要求 ==========

1. L1-L4 审查结果汇总表
2. 完整的策略代码原文（从第一行到最后一行，不省略不摘要）
3. 参数确认模板：
   操作：新建策略
   策略名：
   是否覆盖：否
   策略类型：Code
   编译验证区间：
   初始资金：
   频率：
4. 停下，等待用户确认。未输出完整代码就要求确认的，视为违规。
5. L4 因子逻辑一致性表（如适用）
6. L4 因子合成一致性表（如适用）
7. L4 成本契约一致性表

========== 禁止事项 ==========
- 不要在用户确认前执行任何 jqcli 远端写操作（strategy new/edit/rm, backtest run/rm）
- 允许本地写入 strategies\ 目录，这是审查流程的一部分
- 不要读取、输出 .env、cookie、token
- 不要修改除 strategies/ 目录以外的本地文件
