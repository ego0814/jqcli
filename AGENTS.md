# jqcli 项目规范

## 一、项目定位

本项目的目标：用自然语言描述策略 → AI 生成代码 → 多层审查 → jqcli 写入聚宽 → 回测 → 结果审查 → 归档。

与 Quant Lab 的关系：

- Quant Lab 做研究层（PIT/ASOF、因子构造、数据治理）
- jqcli 做执行层（策略代码写入聚宽、回测、结果抓取）
- 规格书、因子定义、回测结果通过文件对接，两边共用

## 环境与入口约定（2026-09-25）

- jqcli 命令用 `.venv/Scripts/jqcli.exe` 执行（不用 `python -m jqcli.cli`，会触发循环导入）
- 本地测试用 `.venv/Scripts/python.exe -m pytest -q` 执行

本节合并自 `AGENTS.md.bak`（2026-09-25）；该文件保留在仓库根目录作为原始记录。

## 二、六阶段流水线

### 阶段 0：策略意图澄清

- 目标：把自然语言结构化为策略规格书
- 硬规则：九维度逐项确认前，不生成代码
- 产出：specs/<策略名>.md
- 提示词：templates/stage0-clarify.md

### 阶段 1：代码生成 + 4层审查

- L1 语法：Python AST
- L2 静态：qtype 自动检查 + 人工清单，交叉审查
- L3 语义：8条严格自检清单，每条必须给出代码行号和判定
- L4 契约：对照规格书逐条确认
- 硬规则：必须把完整代码贴到对话，用户回复"确认"才能写入聚宽
- 提示词：templates/stage1-generate.md

### 阶段 2：编译验证

- 命令：backtest run --compile --wait
- 停止条件：编译失败 → 回阶段1；通过 → 阶段3
- 禁止：--use-credit
- 提示词：templates/stage2-compile.md

### 阶段 3：正式回测

- 回测参数在阶段0已定，执行时不允许修改
- 产出：回测ID + stats + logs (error + all)
- 提交回测时默认使用 `day` 频率；不传 `--use-credit`（阶段 2 已禁止）
- 提示词：templates/stage3-backtest.md

### 阶段 4：结果审查 + 迭代决策

- 7个维度评估：收益、风险、稳定性、交易行为、样本外、对标、失效场景
- 决策：通过 / 需修改 / 放弃
- 硬规则：没有阶段4决策，不允许进入模拟或实盘
- 提示词：templates/stage4-evaluate.md

### 阶段 4 双引擎规则（2026-09-21）

当策略既有聚宽结果又有本地结果时：

| 指标类型 | 采用 | 理由 |
|---|---|---|
| 收益类（年化、累计、超额） | 两者可交叉验证，差异 <5% | 收益口径可信 |
| 风险类（夏普、最大回撤、Calmar） | **以聚宽为准** | 本地缺撮合细节，风险偏乐观 |
| 换手率 | 分别标注口径 | 聚宽日均口径 vs 本地单次口径，不可直接对比 |
| 门槛判定 | 用聚宽的风险指标 | 本地风险指标不作为定级依据 |

实测依据（ETF 池等权，2026-09-21）：

- 年化差异：+2.3%（主）/ +9.7%（内）/ -3.2%（外）
- 夏普差异：-12.6%
- 回撤差异：+14.9%（本地偏小）

本地回测的定位：

- ✅ 可用于因子/组合层面的快速验证
- ✅ 可用于 CB 这类聚宽不支持的回测
- ❌ 不可替代聚宽做风险定级

### 阶段 5：归档 + 版本管理

- 每次迭代必须记录：策略名、版本、策略ID、代码文件、规格书、回测ID、参数、结论
- 产出：records/<策略名>.md
- 放弃的策略归档到 failures/
- 提示词：templates/stage5-archive.md

### 阶段 5 新增要求：可视化报告

每次策略归档时，必须同时生成一份自包含 HTML 报告：

- 路径：records/<策略名>/report_v1.html
- 生成工具：factor_lab/data/make_report.py
- 内容：指标卡片 + 净值曲线 + 回撤 + 分年度 + 月度热力图 + 换手率 + 四情景对比 + 已知局限
- 形态：单文件，plotly.js 内嵌，双击可打开，无需 server 或联网

理由：

- 归档是"决策证据"，报告是证据的"可读形态"
- 报告跟着 records/ 走，不需要独立的 web 应用
- 不需要启动 server、不需要运维

生成命令：

```powershell
D:\project\jqcli\factor_lab\.venv\Scripts\python.exe D:\project\jqcli\factor_lab\data\make_report.py --strategy <策略名>
```

注：新策略需先在 make_report.py 的 STRATEGIES 表里登记（策略名 → 回测目录 / 归档 md / 报告路径）。

## 三、3个横向机制

### 3.1 数据契约（PIT/ASOF）

- 策略必须明确：前复权/后复权/不复权
- get_fundamentals 必须用 pubDate，不用 statDate
- 财务数据必须考虑公告日，避免 PIT 违规
- 与 Quant Lab 的 PIT 管线对齐
- 交易成本统一引用 specs/costs/standard.md，任何策略规格书不单独定义成本

### 3.2 反模式清单（硬约束）

禁止：

- shift(-1)、lead、look_forward、peek_future、forward_fill_future
- 未过滤 ST、停牌、退市股
- get_fundamentals 不考虑公告日
- 未设 set_order_cost 和 set_slippage
- 覆盖已有策略、删除回测
- 使用 --use-credit
- 读取/输出 .env、cookie、token、密码

### 3.3 与 Quant Lab 衔接

- 规格书用 Markdown（specs/），Quant Lab 可直接引用
- 因子定义导出为 YAML/JSON（specs/factors/），两边共用
- 回测结果导出为 CSV（records/returns/），供 Quant Lab 做归因
- 数据契约统一：PIT 语义、复权方式、频率

## F-score 研究结论（2026-09-20）

F-score 在 A 股 2017-2026 无显著预测力。
四次独立验证一致：原始 IC、正交化 IC、ΔF、Quant Lab FAR。
结论已归档到 failures/fscore_v1_2026-09-20.md。
不再在 F-score 上投入时间。

### 六配置复核结论（2026-09-20）

六种处理配置全部复核，最优（市值中性化）IC 0.0107，仍低于门槛。
F-score 正式结案。

PIT 修复后（2026-09-25）config 6（mv+industry+tradable，21 日）复核：
RankIC 0.0059 → **0.0092**，ICIR 0.141 → **0.229**，期数 114 不变，月均样本 3034.5 → 2641.7。
IC 反而上升（原因见下方「PIT 修复暴露的教训」第 2 条），但 RankIC 0.0092
**远低于通常认为有意义的 IC 水平（约 0.02）**，**结论「无效」不变**。

## 副产物

本轮验证了三个通用模块可用：

- analysis/factor_processing.py（去极值 + 标准化）
- analysis/neutralize.py（市值 + 行业中性化）
- data/compute_ic_processed.py（六配置 IC 分析）

以及收益缓存 ic_returns_cache.parquet（500,363 行），
下一个因子的 IC 分析可直接复用，省去每次约 2 分钟。

## 下一个因子的处理原则

1. 先看各维度（行业、市值、其它）的方差占比
2. 只有确认是噪声才中性化
3. 行业方差 > 30% 时默认开行业中性化
4. 但不要默认"中性化一定改善 IC"

## 因子分析标准流程（2026-09-20 确立）

对任何新因子，IC 分析至少包含：

1. 原始 IC
2. 去极值 + 标准化后 IC
3. 市值中性化后 IC
4. 行业中性化后 IC
5. 市值 + 行业 + 可交易性过滤后 IC
6. 分市值组 IC（5 组）
7. IC 衰减（1/5/21/63 日）
8. 样本外验证

四条原则：

1. 先看行业方差占比，> 30% 默认开行业中性化
2. 但不默认"中性化一定改善 IC"
3. 可交易性过滤默认开（提升 ICIR）
4. IC 衰减是必须的，不能只看 21 日

## 因子 → 策略的接口规范（2026-09-21）

### 一、因子值口径（数据层）

因子值产出时（如 compute_ep.py）必须同时输出两个版本：

- raw 版本：原始因子值（用于归因、诊断）
- neutralized 版本：市值 + 行业中性化后的值

下游（因子研究、预测值表、策略）默认用 neutralized 版本。
raw 版本只用于：

- 归因分析（知道原始暴露）
- 对比诊断（中性化是否改善 IC）

理由：避免"因子研究用中性化、预测值表用 raw"的断层。
EP 的 v1 就是断层案例——研究阶段用中性化（ICIR 0.861），
预测值表用 raw（ICIR 0.275），导致策略赚的是银行 beta。

注意：中性化不一定总是更好。产出两个版本后，由因子研究
对比决定用哪个。但"产出"必须先做。

### 二、选股口径（决策层）

因子值口径 ≠ 策略选股口径。

因子层最优（IC 最高）不等于策略层最优。
必须在生成预测值表前，用 quantile_alpha_scan.py 确认：

- 策略实际选股口径（如 Top 50 = Top 1%）的历史超额 > 0
- 且 t > 2
- 且带流动性门槛（5000 万日均成交额）

**关键：分位扫描必须用"先过滤后选"口径。**

算法对比：

- ❌ 错误（先选后过滤）：全市场排序 → Top N → 剔除不合格
  → 会低估集中持仓的可能性（选出的 Top N 可能是小盘股，
  剔除后剩几只勉强拼凑）
- ✅ 正确（先过滤后选）：全市场过滤门槛 → 可投池 →
  在可投池内排序 → Top N
  → 这才是策略实际能执行的

示例（reversal，21 日收益，115 期；门槛口径 = 过去 20 交易日日均成交额）：

| 口径 | Top 10 只年化超额 | t | Top 20 只年化超额 | t |
|---|---|---|---|---|
| 全市场（不设门槛） | +31.97% | +1.87 | +8.94% | +0.97 |
| 先过滤后选，门槛 2000 万 | -20.89% | -3.10 | -15.11% | -2.42 |
| 先过滤后选，门槛 5000 万 | -18.97% | -2.87 | -11.36% | -1.79 |

注：早期诊断（concentrated_liquidity_check.csv）给出的 -16.04% 已经是
"先过滤后选"（当时在可投池内排序），与本次 -18.97% 的差异来自流动性口径
（当时用调仓日单日成交额，现用 20 交易日日均额），不是排序顺序造成的。
结论方向一致：集中持仓的超额来自买不进的标的。

口径约定：加门槛后的超额以**可投股票池等权**为基准
（不加门槛时以全市场等权为基准），否则会把"小盘整体跑输"误算成因子 alpha；
门槛口径为过去 20 个交易日日均成交额 >= --min-amount（默认 5000 万元）。
工具：factor_lab/data/quantile_alpha_scan.py
参数：--pre-filter --min-amount <门槛> [--lookback-days 20]

如果该口径超额为负，三个选择：

1. 调整选股口径（如改为 Top 20%，但需要更大本金）
2. 换因子
3. 放弃该策略

### 三、硬约束

- 集中持仓（Top ≤ 50 只）要求该口径的历史超额 > 0 且 t > 2
- IC 显著但极端分位为负的因子，禁止用集中持仓实现
- 集中持仓的"超额"如果加流动性门槛后转负，视为流动性幻觉
- 预测值表默认用 neutralized 版本（若因子研究证明 raw 更优，则例外）
- 集中持仓（Top ≤ 50 只）的历史超额验证必须在可投池内进行
- 可投池口径：过去 20 交易日日均成交额 >= --min-amount（默认 5000 万）
- 验证工具：quantile_alpha_scan.py --pre-filter --min-amount <门槛>

### CB（可转债）的 t 门槛分层（2026-09-21）

CB 与股票的差异：

- CB 每期标的 100-250 只（股票 3000+）
- CB 的尾部组合收益噪声更大（Top 10 只的 t 天然低于股票）

两个门槛分开：

- 入场券（决定是否值得跑完整回测）：t > 1.5 且年化超额 > 3%
- 策略通过（决定是否用真钱）：沿用样本外夏普 > 0.5

理由：128 期下 t=1.5 → p≈0.13，作为入场券可接受；
t=1.22 的 p≈0.22 过宽，不作为通过标准。

CB 数据口径：转股价/转股溢价率必须用聚宽 bond.CONBOND_DAILY_CONVERT 的
当日值（PIT 安全）；tushare 的 cb_basic.conv_price 是当前值，不可用于历史。

### 四、反面案例（2026-09-20）

**EP 策略**：

- 因子层：中性化 EP 的 ICIR 0.861（优秀）
- 预测值表：v1 用 raw（ICIR 0.275），v2 用中性化
- 选股口径：Top 50（Top 1%）
- 结果：v1 夏普 -0.182，v2 夏普 -0.471
- 根因：**因子层有效 ≠ 选股口径有效**
- 中性化 EP 的 alpha 在 Top 20%（790 只），Top 50 是负 alpha 区

**reversal 因子**：

- Top 10 只：不加门槛 +31.98%，加 5000 万门槛 -16.04%
- 根因：集中持仓的超额完全来自买不进的小盘股

### 五、检查清单

因子值产出时：

- [ ] 是否同时输出了 raw 和 neutralized 两个版本？

预测值表生成前：

- [ ] 因子研究确认的最优配置是哪个？
- [ ] 策略的选股口径是什么（Top N 只）？
- [ ] 该口径的历史超额是否 > 0 且 t > 2？
- [ ] 分位扫描是否用了 --pre-filter（先过滤后选）？
- [ ] 如果否，调整选股口径或换因子

接策略前：

- [ ] Alpha/Beta 是否独立验证过（不只信聚宽平台的数字）？
- [ ] 持仓行业集中度是否量化过？

## 阶段 4 通过门槛（2026-09-21 修订）

按策略类型分两类门槛，统一 rf=0 口径。
策略类型必须在阶段 0 规格书中标注（因子选股 / 资产配置）。

### 类型 1：因子选股策略

适用于：用因子排序选股，相对基准追求超额（如 CB 双低、EP 选股）。

门槛：

- 样本外超额 IR > 0.5
- 样本外超额 t > 1.5
- 样本外超额 > 0（不衰减）
- 保守成本情景仍正

### 类型 2：资产配置策略

适用于：多资产分散持有，吃 beta（如 ETF 池等权）。

门槛：

- 样本外夏普（rf=0）> 0.5
- 样本外超额 > 0（不衰减）

### 口径说明

- 统一使用 rf=0（与无风险利率假设无关，跨资产可比）
- 历史策略用 rf=3.83% 的口径需换算：夏普(rf=0) = 夏普(rf>0) + rf / 波动率

### 为什么分两类

- 资产配置策略的收益来自多资产 beta，不是 alpha；
  用 IR/t 评判不适用（超额可能不显著）
- 因子选股策略的价值在相对基准的超额；
  用绝对夏普评判会被资产的天然波动惩罚
- 用同一套门槛会系统性误判其中一类
- 旧门槛"夏普 > 0.5（rf≈4%）"实际只对低波动资产友好，
  对 CB（波动 12-13%）与股票（25%）系统性偏严

### 历史策略的重判

| 策略 | 类型 | 旧门槛结果 | 新门槛结果 |
|---|---|---|---|
| ETF 池等权 | 资产配置 | 通过 | 通过（夏普 rf=0 1.579 > 0.5） |
| CB 双低 | 因子选股 | 未通过（夏普 rf=3.83% 0.28 < 0.5） | 通过（IR 0.62 > 0.5, t 1.53 > 1.5） |

注（2026-09-22）：上表"新门槛结果"沿用**门槛修订时（2026-09-21）的重判口径**。
启用信用排雷极轻档后（规格书 v1.4、本地回测默认 `--credit-filter on`），
样本外 IR **0.836** / t **1.620**（关闭信用过滤的基线为 IR 0.895 / t 1.733），
仍满足因子选股型门槛（IR > 0.5、t > 1.5）。

## 已验证因子 + 策略（2026-09-22）

| 名称 | 类型 | 状态 | 归档 |
|---|---|---|---|
| F-score | 因子 | 无效 | failures/ |
| EP | 因子 | 有效，形态不匹配小额资金 | records/ |
| reversal | 因子 | IC 有效，alpha 不可投（**归档值不可追溯**，见 failures/reversal_v1_untraceable.md） | records/ |
| ETF 池等权 | 策略 | 通过（资产配置型） | records/ |
| CB 双低 | 因子选股策略 | 通过（v1.4：2026-09-22 起默认启用信用排雷极轻档；样本外 +9.18% / IR 0.836 / t 1.620；换手 37.7% 为已知局限） | records/cb_double_low/ |

注：**CB 双低的本地回测默认带信用排雷**（--credit-filter on --credit-tier light，规格书 v1.4）。
传 --credit-filter off 可复现 v1.3 及更早结论；比较新旧回测结果时必须确认该开关，否则口径不一致。

**股票端集中持仓结论（2026-09-21）**：
EP 和 reversal 在可投池内的 Top 10-50 只超额全部为负。
10 万本金的股票端集中持仓路径已排除。
alpha 只在 Top 20%-40% 宽分位（数百到上千只，需 500 万+ 本金）。

三个因子的最终定位（2026-09-20）：

- F-score：因子层无效，已结案 → failures/
- EP：因子层强有效（63 日 ICIR **0.723**，PIT 修复后；历史口径 1.052 → 0.861 见本段末注），
  但 alpha 只在 Top 20% 宽分位
  （790 只，年化超额 +5.71%，t=3.53）；Top 50 口径年化超额 -5.21%
  → 形态不匹配 10 万本金
- reversal：因子层 IC 有效（ICIR 0.510，分市值组 5 组全正），
  但集中持仓超额加 5000 万流动性门槛后由 +31.98% 转为 -16.04%
  → alpha 依赖买不进的标的，不可投
- ETF 池等权（**策略**，2026-09-21）：7 只低相关 ETF 等权 + 季度再平衡，
  三段区间年化 +9.31% / +4.93% / +16.00%，夏普(raw) 0.823，最大回撤 17.03%
  → 通过，但属资产配置型（收益来自多资产 beta），再平衡贡献 -0.18pp/年
  → records/etf_pool_v1.md

结论：三个因子都没有 10 万本金可用的集中持仓形态，股票端方向转为 ETF。

EP 的换频测试（2026-09-20）显示季度调仓（63 日）ICIR 1.052、
t=11.71，显著优于月度调仓（0.652）。6 个配置全部改善 59%-79%。
策略层用 EP 时应采用季度调仓。

注：EP 的 63 日 ICIR 经过三次口径修订，1.052 与 0.861 均保留为历史对照：

| 版本 | ICIR | 说明 |
|---|---|---|
| v1 | 1.052 | tradability 向量化前 |
| v2 | 0.861 | 向量化后、PIT 修复前（AGENTS.md 原复核口径） |
| **v3（现行）** | **0.723** | PIT 修复后：流动性窗口由「未来 21 日」改为「过去 21 日」 |

v3 的下降 **100% 来自流动性窗口口径，与涨停剔除无关**。
**ICIR 是 IC 的信息比率，与阶段 4 的样本外超额 IR 不是同一口径。**
EP 仍为强有效，其 63 日 ICIR 的现行口径是 0.723。


## PIT 修复暴露的教训（2026-09-25）

1. **未来窗口抬高 IC**：修复前 `windows()` 返回 T+1 起窗口，
   `filter_tradable()` 用「下期首日涨停 / 下期末日跌停 / 下期停牌 / 下期日均成交额」
   决定样本入选，属于**样本层面的前视**。隔离实验：旧流动性窗口 IC 0.049695
   vs 新窗口 0.031576，差 0.018119 = 旧水平的 36.5%。现行：筛样本只能用 T 日收盘及之前的信息；
   执行可行性诊断（T+1 涨停买不进、T 期末跌停卖不出）由 `future_windows()` 单独提供，不得用于筛样本。
2. **归因须做隔离实验**：EP 的 config 6 IC 下降 37.72%，第一反应会归因到「涨停剔除」；
   实测 `LIMIT_UP_T1` 只占旧剔除总数的 3.77%，把旧口径全部剔除项加回后 IC 仍是 0.050051，而新口径是 0.031244，
   S2−S3 = +0.018119。**EP 的 IC 下降 100% 来自流动性窗口口径。**
   反向也出现过：fscore 的 IC 反而上升 55.82%（旧口径用 T+1 涨停剔除了远期收益分布上端的标的，
   截断压缩了排序差异），说明「修复后 IC 一定下降」是错误预期。
3. **ICIR ≠ 阶段 4 的 IR**：ICIR = IC 均值 / IC 标准差 × √n，衡量截面预测稳定性；
   阶段 4 的「样本外超额 IR」= 策略相对基准的月度超额收益均值 / 标准差 × √12。
   两者数值区间接近，极易被混用，引用时必须写明是哪一种。
4. **ST 未知状态必须 fail-closed**：`make_cb_predictions.py` 原来 `st_flag` 未知时默认 False
   （按非 ST 处理），`tradability.py` 的 ST 未命中同样保持非 ST——这是 fail-open。现行：未知一律不合格。
   CB 侧前提是「未知占比 ≤ 1%」；当前 58 只持仓与历史 30,042 行的未知占比均为 0%，
   所以这次修正是空操作，但它是通道性风险。
5. **双引擎对比的基数效应**：ETF 池等权样本内年化 本地 +5.41% vs 聚宽 +4.93%，相对差 +9.74%，
   看似超出「收益口径 <5%」的预期；但绝对差只有 0.48pp，因为样本内区间总收益仅 +30.55%。
   **规则：双引擎对比必须同时给出绝对差与相对差，并标注区间总收益量级。**

## ETF 扩池尝试（已证伪，2026-09-25）

| 阶段 | 池规模 | 结果 |
|---|---|---|
| 20 只矩阵 | 20 | 9 对相关系数 > 0.8；最高 510050-510300 = 0.9367 |
| 去掉 510050/515000/511020 | 17 | 仍 5 对 > 0.8；最高 510500-159915 = 0.8663 |
| 穷举 6188 组合取最小最大相关 | 12 | **池内最大相关 0.6806** > 0.6 门槛 |

- 从 17 只里选，满足「池内最大相关 ≤ 0.6」的最大规模是 **11 只**（最优 0.5405），不是 12 只
- 12 只的 0.6806 是 6188 个组合里的精确最小值，「12 只且 ≤0.6」在数学上不可行
- 若强制样本 ≥2000 行（排除 588000/159980/512890），12 只的最优最大相关恶化到 0.8387：低相关组合必须靠短样本的科创50 与有色替换重叠的沪深300/中证500/创业板
- 原 7 只池同源重算的池内最大相关 0.5040（逐对）/ 0.5126（共同区间），优于 12 只池的 0.6806

**结论：原 7 只池（max 0.50）已是 ETF 端分散化的最优解；双动量方向放弃；ETF 池等权（已通过）是 ETF 端唯一部署策略。**
归档：records/etf_pool_expansion_failed.md；证据：local/data/etf_pool_17/ 与 etf_pool_20/。
注：从 17 只里重选 7 只可把样本内最大相关压到 0.2192，但那是样本内最小化相关性的结果，
丢掉了中证500与中概互联、把 A 股暴露全压在科创50 上，有过拟合风险，不作为结论。

## 编码坑：禁用 PowerShell 做中文字面量替换（2026-09-25）

PowerShell 对含中文字面量的**替换型**命令会篡改字符
（实测：把 `注` 改成 `：`、把 `：` 改成 `意`，且是**全局**替换而非定向替换）。因此：

- 不得用 PowerShell 的 `-replace` 或 `.Replace()` 处理包含中文的替换
- 不得用 `@(...)` 数组对 + 中文字面量构造替换表
- 文档编辑必须用 **Python 精确行替换**：`readlines()` 读入、按行号整行替换、`writelines()` 写回；禁止用 `str.replace()` 做子串替换
- 每次写入前先备份到 `local/backups/`，写入后立即校验（行数 / 目标行内容 / 字节数）；校验失败则立即从备份恢复并停止

## 可交易性过滤的 ST 分支

ST 股的涨跌停阈值是 5%（非 ST 主板 10%、创业板/科创板 20%、
北交所 30%）。这不是可选项，是必需项：

- ST 样本中 7.6% 的行会触发 5% 涨跌停判定
- 用 10% 阈值会漏判 99.8% 的 ST 涨跌停
- 漏判的后果：回测会把"买不进/卖不出"的行当作可交易，
  高估策略收益

如果上游已剔除 ST（如 F-score 的 panel），这条分支不会被触发，
但代码不能省。一旦研究 ST 股策略，这条分支就是唯一的正确路径。


## 性能瓶颈基线（2026-09-20）

全量 --ic-decay（5,628 只股票）各阶段耗时：

- build_returns（单周期，未 hoist）：490s
- evaluate 6 配置 IC：279s
- build_multi_returns px 加载：10.57s（已 hoist）
- load_aux + 其它：< 15s

真正的瓶颈是 build_returns 未做 px hoist，以及
evaluate 的 groupby.apply 回调。


## 聚宽回测环境 API 差异（2026-09-21）

本项目的聚宽回测环境（jqboson）不支持 order_target_percent。
用 order_target_value 替代：

```python
value_each = context.portfolio.total_value * weight
order_target_value(code, value_each)
```

已验证可用：order_target_value、order_target、set_order_cost、
set_slippage、get_current_data、attribute_history、get_extras、
get_security_info、read_file。

已验证不可用：order_target_percent（编译记录 63332982：
NameError: name 'order_target_percent' is not defined）。

### 补充发现（同日，编译记录 63333109）

100 股最小交易单位 + order_target_value 的目标差额 < 1 手时，
委托会被调整为 0 手并报错「开仓数量不能小于 100 / 平仓数量不能小于 100」。

- 单价越高的标的越严重：511260（单价 100~134 元，1 手约 1.0~1.4 万元）
  在 10 万本金下 34/36 次调仓失败，等于债券槽位实际未持有
- 7 只 ETF 中只有它单价在百元级，其余 0.7~9 元，1 手仅 70~900 元
- 排查方法：读 backtest logs，统计「订单委托失败」按标的分布
  （编译记录 63333109：66 条失败中 49 条开仓、12 条平仓、5 条资金不足）

结论：本金 < 50 万时，池内不允许出现"单价 × 100 ≥ 目标金额的一半"的标的，
否则该槽位形同虚设。

## 回测引擎一致性（2026-09-21）

本地回测（factor_lab）与聚宽回测是两套独立引擎。同一策略两边都能跑时，
必须以聚宽结果为基准做交叉验证并记录差异；只有本地能跑时（如 CB），
必须在归档中标注"单一引擎"。

### 双引擎差异实测（2026-09-21）

ETF 池等权在聚宽和本地的对比（成本参数一致：佣金万0.854 + 最低5元 + 滑点千0.5）：

| 指标 | 聚宽 | 本地 | 相对差 |
|---|---|---|---|
| 主回测年化 | +9.31% | +9.53% | **+2.3%** |
| 样本内年化 | +4.93% | +5.41% | +9.7% |
| 样本外年化 | +16.00% | +15.49% | **-3.2%** |
| 样本外夏普（raw） | 1.579 | 1.380 | -12.6% |
| 样本外最大回撤 | -11.21% | -9.54% | +14.9% |

结论：**情况 B（可解释差异）** —— 年化两项 <5%，三项落在 5-15%，无一项超过 15%。

差异来源（按影响大小排序）：

1. **QDII 溢价过滤**（聚宽有、本地无）：聚宽策略在 19 期因 513100/513050 的
   T-1 溢价 >2% 跳过买入；本地无 IOPV/净值数据，未实现该过滤
2. **价格数据源**：本地用 Tushare `fund_daily` + `fund_adj`（后复权）与 AkShare；
   聚宽用其自有行情与复权口径
3. **撮合逻辑**：本地按 `pct_chg` 逐日复利近似；聚宽为平台撮合（真实成交价 + 滑点模型）
4. **交易时点**：本地按收盘价计算净值与调仓；聚宽策略在开盘执行
5. **换手率定义不同（不可直接比较）**：聚宽 `turnover_rate` 0.127%（平台口径）；
   本地 4.94%/次调仓（季度、单边）

可接受性判断：

- **年化收益差 <5%** → 本地引擎可用于因子与组合层面的收益验证
- **风险指标差 12-15%（夏普/回撤）** → 本地不能替代平台做最终风险定级；
  阶段 4 的夏普/回撤结论仍以聚宽为准
- 本地回测的定位：快速迭代与因子筛选；聚宽回测的定位：最终决策证据

## 打点标签修正（2026-09-20）

原 "[timing] 收益推导+数据缓存" 打点位置在 IC 循环之后，
使用的是累计时间（time.time() - t_start），语义与名称不符。
修正后移到了 if/else 之后，标签改为"收益获取（缓存或推导）"。

修正后的真实瓶颈分布（F-score 全量）：

- 收益获取（缓存命中）：< 1s
- IC 计算（6 配置）：约 280s
- load_aux + 加载 px：约 12s

之前的"收益推导 490s"是误读：490s 里含 IC 的 279s。


## tradability 向量化基线（2026-09-20）

- filter_tradable：568s → 85s（6.7×），修复 2 个数据缺陷
- tradability_audit：已向量化，与旧版计数 10/10 一致


## 四、确认机制

### 必须用户确认的操作

- strategy new（新建策略）
- strategy edit（更新策略）
- strategy rm（删除策略）
- backtest run（正式回测，非 --compile）
- backtest rm（删除回测）
- 任何覆盖已有内容的操作

### 可以自动执行的操作

- 所有只读命令：strategy ls/show, backtest ls/show/stats/logs, research ls
- 本地文件写入（AGENTS.md、specs/、strategies/、records/、templates/、failures/）
- Python 语法检查
- qtype 本地检查

### 确认模板

操作：新建/更新/删除/回测
策略名：
策略ID：
是否覆盖：
回测区间：
初始资金：
基准：
频率：

## 五、L2 静态审查（交叉审查）

L2 由两部分组成，两部分都要执行。

### L2a：qtype 自动检查

路径：D:\project\jqcli\.venv\Scripts\qtype.exe
命令：D:\project\jqcli\.venv\Scripts\qtype.exe check .\strategies\<文件名>.py --format json

- 返回 [] 表示通过
- 非空数组则逐条报告：规则ID、行号、消息
- QT001/QT002（error级）必须修复后重新检查
- QT003/QT004/QT005（warning级）需修复或说明理由

### L2b：人工清单（覆盖 qtype 盲区）

无论 qtype 是否通过，都执行：

1. 搜索所有 .shift( ，确认参数无负数
2. 搜索 lead/look_forward/peek_future/forward_fill_future，确认不存在
3. 检查股票池构造，确认过滤 ST/停牌/退市/上市时长
4. 检查所有 .merge( ，确认显式指定 on=/left_on=/left_index=
5. 检查 pct_change() 的赋值目标，确认未赋给 forward_*/next_*/target

两部分都 PASS 才能进入 L3。

## 六、L3 严格自检规范

L3 语义审查共 8 条，每条必须给出代码行号和代码原文，不允许无证据结论。判定只能是 PASS / FAIL / NA。任何 FAIL，必须修复后重新执行整轮 L3。以下表述视为违规：

- "已检查，无问题"
- "符合预期"
- "逻辑正确"
- "没有发现明显问题"

具体规则见 templates/stage1-generate.md。

## 七、文件命名约定

- 规格书：specs/<策略名>.md（如 specs/ai_momentum_300.md）
- 代码：strategies/<策略名>.py
- 归档：records/<策略名>.md
- 失败归档：failures/<策略名>_<日期>.md
- 因子定义：specs/factors/<因子名>.yaml

## 八、版本管理

每次策略迭代：

- 规格书版本：v1 → v2 → v3
- 代码对应版本：与规格书版本一致
- 回测ID逐个记录，不覆盖
- 失败版本归档到 failures/

## 月度模拟盘自动化（2026-09-22）

CB 双低实盘模拟闭环：每月末最后一个交易日（含其后 3 天补跑窗口）自动刷新行情、
生成信号 + 复盘。

| 项 | 值 |
|---|---|
| 入口脚本 | factor_lab/data/run_monthly.ps1 |
| 调度 | Codex automation（id cb，每月 20-31 日 22:30 触发，project jqcli） |
| 手动补跑 | powershell -NoProfile -ExecutionPolicy Bypass -File ...\run_monthly.ps1 -Force [-Capital N] |
| 产物 | output/monthly_signals/<日期>.csv（目标清单）、<日期>_sell.csv（卖出）、TRACKING.md（台账） |
| 日志 | output/factor_values/monthly_run_<时间戳>.log |

三道守卫（使多次触发可安全重复、且支持跨天补跑）：

1. 交易日历覆盖检查 —— 过期自动调用 factor_lab/data/refresh_trade_cal.py
2. 调仓窗口 —— 目标交易日 = 本月最后交易日（已到或已过）；若本月最后交易日尚未到，
   则回退到上一个自然月的最后交易日（跨月补跑）；且 今天 − 目标交易日 <= MaxCatchupDays
   （默认 3 个自然日，`-MaxCatchupDays N` 可调）
3. 目标月份尚未生成过信号文件（output/monthly_signals/<YYYY-MM>-*.csv）

为什么不是"今天必须正好是月末最后交易日"（2026-09-22 修订）：

- 22:30 触发时当日行情可能仍未落库，跨天补跑能等到完整数据
- 机器在月末当天关机/睡眠时，次日仍能补跑
- 超出补跑窗口则跳过，避免用过期行情生成信号

判定实测（真实交易日历）：

| 今天 | 判定 | 目标交易日 |
|---|---|---|
| 2026-08-29（非月末） | SKIP（gap 29） | 20260731 |
| 2026-08-31（月末） | RUN（gap 0） | 20260831 |
| 2026-09-22（今天） | SKIP（gap 22） | 20260831 |
| 2026-09-30（月末） | RUN（gap 0） | 20260930 |
| 2026-10-01 / 02 / 03 | RUN 补跑（gap 1 / 2 / 3） | 20260930 |
| 2026-10-04 | SKIP（gap 4，超窗口） | 20260930 |
| 2026-11-01（跨月） | RUN 补跑（gap 2） | 20261030 |

脚本编码必须是 UTF-8 with BOM：Windows PowerShell 5.1 按 GBK 解析无 BOM 的 .ps1，
中文字符串会破坏引号配对并报 Missing closing '}'。

### 增量刷新机制（2026-09-22 上线）

run_monthly.ps1 的 [0/3] 步调用 download_cb.py --incremental：

- cb_daily：读缓存 max(trade_date)，按 trade_date 从 max+1 拉到今天，逐日拉全市场
  （单日约 314 行 / 0.5s；逐只全量要 1,100 次请求 / 数分钟）
- 单日接口实测是缓存行的严格超集（多出 118059.SH / 123175.SZ / 127068.SZ）
  → 增量模式同时补全量模式漏掉的标的
- 同时刷新 cb_basic（1 次请求）与 cb_redeem_jsl（AkShare 快照）
- 合并规则：concat + drop_duplicates(ts_code, trade_date, keep=last)，不产生重复行
- 刷新失败不阻塞：沿用旧缓存，日志标红，摘要注明"信号基于 <日期>"
- -SkipRefresh 跳过刷新（调试）；-Force 跳过守卫（手动补跑）；-MaxCatchupDays 调补跑窗口

仍不可自动化：cb_convert_*（聚宽 bond.CONBOND_DAILY_CONVERT，PIT 转股价/溢价率）
只能在聚宽研究环境抓取回传后 `python download_cb_convert.py --decode <年份>`；
download_cb.py 会检查滞后并在滞后时打印该指引（Tushare 无对应权限）。

### 双层调度（2026-09-22 上线）

| 层 | 触发 | 作用 |
|---|---|---|
| Codex automation（id cb） | 每月 1-3 日 + 20-31 日 22:30 | 带 agent 判断：读日志、按规则报告（跳过则安静） |
| Windows 任务计划「CB双低-月度模拟盘」 | 每日 22:30 | 纯脚本执行，不依赖 Codex App 是否打开 |

两层都调用同一个 run_monthly.ps1，靠脚本守卫（守卫 1 调仓窗口 + 守卫 2 目标月份已有信号）
保证幂等，重复触发不会重复生成信号。Windows 任务设置：Interactive only（需用户登录）、
允许电池运行（AllowStartIfOnBatteries）、错过后补跑（StartWhenAvailable）、超时 2 小时、
powershell -WindowStyle Hidden。触发时间定在 22:30 是给当日行情落库留时间
（2026-09-22 实测：21:00 前后当日 cb_daily 常仍未更新）。Windows 任务已于 2026-09-22
用 schtasks /Run 手动触发验证：Last Result = 0，并产出 monthly_run_*.log。

### 两层调度语义一致性（2026-09-22）

Codex automation 的 rrule 已扩展为 BYMONTHDAY=1,2,3,20-31：

- 20-31 日：覆盖月末正常触发
- 1-3 日：覆盖跨月补跑（如上月末信号未生成）

两层调度现在的语义：

| 层 | 触发范围 | 兜底能力 |
|---|---|---|
| Codex automation | 每月 1-3 日 + 20-31 日 22:30 | 跨月补跑（若 Windows 任务失效） |
| Windows 任务 | 每日 22:30 | 全部（每日都醒，守卫判定） |

守卫逻辑保证：非调仓窗口静默跳过，不重复生成。

两边语义对齐后，月末信号有两条独立路径兜底；重复触发由守卫 1（调仓窗口）+ 守卫 2
（目标月份已有信号）拦掉，不会产生重复信号或重复台账记录。

编码坑（实测）：PS 5.1 下 python 子进程输出与 PowerShell 解码必须同时指定 UTF-8
（脚本内设 PYTHONIOENCODING/PYTHONUTF8 + [Console]::OutputEncoding），否则日志里的
中文会变成 U+FFFD 乱码。Tee-Object 在 PS 5.1 下写出的是 UTF-16LE 日志。
